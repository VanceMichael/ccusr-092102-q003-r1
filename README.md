# 晋戏影像沿革档案

本项目服务于山西戏曲长期影像资料的整理与开放。领域资料将影像文件、拍摄地点、剧种分类、人物别名、说明文字和传播许可视为相互独立但可追溯的记录。

运行检查：

```bash
python3 -m unittest discover -s tests -v
```

`fixtures/archive_item.json` 是一条最小影像记录。真实资料中的原始文件指纹、早期说明和后续勘误都应长期保留。
